首先使用函数strcmp比较s1与s2的大小，并且用*p1，*p2分别记录s1，s2在S里的位置，通过函数strstr得到p1,p2。
然后比较p1,p2的前后顺序，由于得到的p1与p2都是S中出现s1,s2的首个字母的位置,因此需要将前面的地址加上该字符串长度，后面的地址减一，得到完整的中间帧字符串。
通过函数将中间帧字符串存入新数组中，然后进行字符处理，只保留26个字母，并且转变大小写。
字符本身即为整形，直接对数组进行处理，由于0也需要输出，所以将数字/10三次，并对10取三次模，逆序处理数组，即可得到结果。但是需要将最后一个数字单独处理，以舍去多余的0。